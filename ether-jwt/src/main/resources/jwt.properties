# Optional legacy compatibility settings for JWebTokenImpl.
# Prefer programmatic configuration with JwtConfig + KeyProvider.
#jwt.algorithm=HS256
#jwt.secret=change-me
#jwt.privateKeyPath=/path/to/private_key.pem
#jwt.publicKeyPath=/path/to/public_key.pem
